/// scr_Player_Input()

// Movement Input
Key_L = keyboard_check_pressed(vk_left);
Key_R = keyboard_check_pressed(vk_right);

// Drop Speed Input
Key_D = keyboard_check(vk_down);

// Rotation Input
Key_U = keyboard_check_pressed(vk_up);
